/** Host loader entry for the git-manager tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the git manager tab. */
export function apply(): void {}
